export default function useData(
  challengeId: number,
  filters: {
    currentStage: string;
    sortBy: string;
  }
) {
  // TODO: fetch submissions from backend, according to the challenge id and filters

  // returns data
  return [];
}
