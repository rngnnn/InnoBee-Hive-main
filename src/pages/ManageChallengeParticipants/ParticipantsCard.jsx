import React from "react";
import UserCard from "../../components/ProfileComponent/UserCard";

const ParticipantsCard = ({ item, index }) => {
  return <UserCard index={index} />;
};

export default ParticipantsCard;